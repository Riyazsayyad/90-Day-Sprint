# Constructors & class initialization instruction track

The user requested a lesson on "Constructors in Java" from an interview and software engineering perspective (via `/teach Constructors in Java`). This covers why constructors exist (invariants enforcement), compiler default constructor generation rules, constructor overloading, constructor chaining via this() and super() constraints, execution flow under inheritance, why constructors cannot be inherited or overridden (lack of dynamic dispatch), static vs. instance initialization blocks, and interview checks.

**Evidence:** Explicit user request: `/teach Teach me "Constructors in Java" from an interview and software engineering perspective.`

**Implications:** Detail compiler behavior with default constructor generation (and how it ceases when user constructors are declared). Show constructor chaining order and the first-line syntactic constraint. Clarify static vs instance initialization order to build foundations for nested object structures.
