# Abstraction & Interface Contracts instruction track

The user requested a lesson on "Abstraction in Java" from an interview and software engineering perspective (via `/teach Teach me "Abstraction in Java" from an interview and software engineering perspective.`). This covers why abstraction exists (managing complexity, OCP), abstraction vs. encapsulation differences, abstract class structures (partial implementation, constructors, instance state restrictions), interfaces high-level boundaries (behavior contracts, skeletal implementations), decoupling APIs using abstractions, framework interfaces (Spring, JDBC), common interview questions, misconceptions, and best practices.

**Evidence:** Explicit user request: `/teach Teach me "Abstraction in Java" from an interview and software engineering perspective.`

**Implications:** Ground the student in designing loose-coupled, mockable API boundaries using abstractions. Clarify how abstract classes allow sharing state while interfaces represent pure behavior contracts. Introduce skeletal implementation patterns (combining interfaces and abstract classes) as a best-practice framework design pattern.
