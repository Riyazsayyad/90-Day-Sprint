# Collections Utility instruction track

The user requested a lesson on Java's `java.util.Collections` utility class (via `/teach Collections Utility`), expanding the study of Java collection internals. This introduces static collection algorithms, wrapper mechanisms (unmodifiable, synchronized, and checked collections), and sentinel empty/singleton factories.

**Evidence:** Explicit user request for `/teach Collections Utility` covering wrappers, static methods, and internals.

**Implications:** Ground the lesson in the structure of collection wrappers (views vs copies), thread synchronization mutex boundaries, and type safety constraints. Draw clear distinctions between utility wrappers and immutable collections (like `List.of()`).
