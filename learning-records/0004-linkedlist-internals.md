# LinkedList internals instruction track

The user requested a lesson on LinkedList internals (via `/teach 2. LinkedList`), which was created alongside ArrayList internals. It establishes a comparison framework between contiguous memory arrays and node chains.

**Evidence:** Explicit user request for `/teach 2. LinkedList ...`.

**Implications:** Use the contrasting styles of ArrayList ($O(1)$ lookup, $O(n)$ shift) and LinkedList ($O(n)$ lookup, $O(1)$ link modifications) as foundational knowledge. Prepare for comparative discussions or a mixed quiz in future lessons.
